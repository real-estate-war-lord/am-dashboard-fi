# V1 — Parity audit (docs only) + harness

1. Compare the Danish v3.0 (`docs/v2_1/ref/UI_SPEC_v3.md` incl. amendments, `ref/UI_V3.md`, `ref/QA.md`,
   `ref/PROGRESS.md`, `ref/DK_P10.md`, the code in `ref/dk_src/`) with this repo's current v2.0 behaviour
   (read `docs/UI_PLAN.md`, the code, and run the gate for screenshots). Write `docs/v2_1/PARITY_AUDIT.md`:
   one row per Danish behaviour / component / AC — `DK item · FI status (done | partial | missing | n/a for Finland)
   · evidence (file:function or screenshot) · target phase (V2–V6) · MUST/SHOULD`. Include at least: nav + redirects,
   IndicatorPicker (search, groups, "From the municipality", keyboard), PeriodControl modes, Layers ▾ (every layer
   one switch, state in lay=), unified search, map pin behaviour (DK P10 §1), legend stacking, map area card,
   area-page study row (no full rebuild on chip change, scroll kept), chart-panel modes, headline tiles
   (clickable, inherited label), minimap drag/full screen/teardown, Test property (study row, 8 sections, services
   layer, full indicator list incl. Climate, radius, export), Export ▾ (long schema, unit assertion), Data tabs,
   sheets (no grey filler, breadcrumbs), number rules, responsive (drawer, no overflow), accessibility sweep,
   empty/loading/error states. Mark n/a what cannot exist in Finland (National series, horizons 2070/2120).
2. Distribute every MUST gap to V3–V6 in the audit (V2 is fixed: colour ramps). Anything that does not fit goes
   to a "Later" list at the bottom.
3. Create `docs/v2_1/PROGRESS.md` and `docs/v2_1/DECISIONS.md` if missing.
4. Harness: confirm `tests/ui_v2.spec.py` PHASES contains V1…V7 (the wrapper added them). Add one check
   `v21_audit_exists` (phase V1) that asserts the audit file exists and has ≥ 40 table rows.
No app code changes in V1.
