# V7 — Final QA, docs, release notes

1. Run the whole gate; take `SHOTS=1` screenshots; look at every route at 1440 and 390 and fix what looks wrong.
2. Re-read `docs/v2_1/PARITY_AUDIT.md`: every MUST row must now be done (evidence = check id) or explicitly moved
   to Later with a reason.
3. Write `docs/v2_1/QA.md` (remaining issues, none blocking), update `README.md` screenshots section,
   `CHANGELOG.md` v2.1, `docs/UI_PLAN.md` (routes/keys: `pin=`, `lay=services` on property, ramp rule).
4. Write `docs/v2_1/RELEASE_NOTES_FI.md` **in Finnish** for the owner: top 5 things to look at with their URLs,
   the colour rule with an example, what was left out and why, and the one release command
   `./overnight.sh release`.
