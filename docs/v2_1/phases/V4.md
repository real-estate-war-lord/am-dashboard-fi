# V4 — Test property: layers, services, full indicator list

1. DK P10 §2: Services layer (groceries, food & drink, pharmacies, public-transport stops — the FI sources already
   used on the macro map) on the property mini-map via its Layers ▾, same category filters, own legend, per-map
   panes/renderers, `lay=services` + filter keys in the property hash, default off.
2. DK P10 §3: every layer drawn on the property map (infra, public buildings, buildings, services, flood zones,
   radius rings) has exactly one switch in Layers ▾; switching off removes markers and legend at once; the async
   loaders never re-add a layer that is off; the state survives re-render and reload (`lay=`).
3. DK P10 §4: the property picker offers the same choice as an area page of the same place: the pin's finest
   level's own indicators first, then "From the postal code" / "From the municipality" with the `muni` tag —
   Climate included; choosing a Climate indicator shows the return-period control, climate bars in the chart
   panel and the SYKE zones in the mini-map.
4. Every MUST gap the audit assigns to V4 (sections, radius, export, empty state).
5. Checks (phase V4), on `#property?p=60.2448,24.8665`: services toggle on → legend-services inside the mini-map,
   off → gone; public off → no public markers and no legend-public, still off after reload; picker has a Climate
   group and ≥ 45 rows; a Climate indicator shows the return-period control and legend-zones.
