# V5 — Area page, sheets, Data and Export gaps

Everything the audit assigns to V5, typically: study row updates in place on chip/tile change (no full rebuild,
scroll and minimap zoom kept); chart-panel modes complete (history, snapshot/distribution strip, outlook, climate
bars per return period); tiles clickable with inherited labels; toggles state in `show=`; sub-areas table; sheets
(project, public building, school, climate) with no grey filler and correct breadcrumbs; Data › Areas / Projects /
Sources parity; Export ▾ parity (long schema, value_type, inherited_from, unit assertion, projects and test-property
files). One check per item (phase V5).
