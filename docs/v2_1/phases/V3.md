# V3 — Map: pin stays on the map + map gaps from the audit

1. DK P10 §1 (`docs/v2_1/ref/DK_P10.md`), adapted: pasting a Google Maps link, `lat, lon` or picking an address in
   the unified map search **drops a pin on the macro map** (pan, zoom ~13, 1 km radius rings) and stays on `#map`.
   Pin card (`data-testid=pin-card`): label, coordinates, osa-alue › postinumero › kunta, **`View test property ›`**
   (`pin-open` → `#property?p=…`) and `×`. Search row text "Drop a pin here" (keep `search-coord`). The pin card
   never overlaps the toolbar or the legend stack (check 1440, 1366, 390). The Test property page's own box keeps
   replacing its pin in place.
2. Every MUST gap the audit assigns to V3 (map toolbar, Layers ▾, legends, map area card, unified search).
3. Checks (phase V3): pasting `60.2448, 24.8665` keeps the hash on `map` with `pin=`, pin card visible,
   `pin-open` lands on `#property?p=60.2448,24.8665`; the audit's V3 items each have a check.
