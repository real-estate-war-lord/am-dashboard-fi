# Changelog

## v2.5.1 — 2026-09-24

A corrective patch. v2.5 made the map choose the area level from the zoom; in use that was wrong,
because it meant **zooming changed what was selected**. Panning or scrolling could silently drill
into a municipality, swap the choropleth to buildings and rewrite the URL under the reader. Zooming
is a camera movement, not a selection, and v2.5.1 restores that separation.

- **The automatic zoom-tied granularity ladder is removed.** Wheel, trackpad, pinch, `+` / `−` and
  double-click now change the view and nothing else: never the selected area, the breadcrumb, the
  level or the indicator, and they never open a popup. Selection happens only when the reader asks
  for it — clicking an area, the breadcrumb, the search box or the Areas / Buildings control. The
  v2.4 drill-in is back exactly as it was: clicking København still opens its **67 quarters**.
- **Quick jumps instead**: **Copenhagen** and **Denmark** buttons in the map toolbar, with the
  keyboard shortcuts **C** and **D** (ignored while typing in any input, and never with a modifier
  held). They move the camera only — `fitBounds` and nothing more. Copenhagen frames **København +
  Frederiksberg** together, because the two read as one city; Denmark frames the whole country.
  Neither selects a municipality, touches the breadcrumb or the indicator, or opens a popup.
- **Kept from v2.5**: the test-property radius filter, the faster canvas-and-cluster Public layer,
  and the fix that keeps the Services overlay drawn in Buildings mode.
- **Fixed — the Leaflet canvas crash, at the root.** `Cannot read properties of undefined (reading
  'save')` came from `_redraw()` running on a canvas renderer that was not on a map: its `_ctx`
  exists only between `onAdd` and `onRemove`, so a redraw landing just after a map teardown — an
  async building, services or public file resolving, a filter applied mid-rebuild — drew into
  nothing. Reproduced exactly by redrawing a renderer that was never added, and one that was
  removed. Guarded once on `L.Canvas.prototype`, so it holds for the macro map, the area map, the
  Analysis mini map and any future one, while a live renderer still draws normally. Layer groups
  are now dropped and forgotten in one step (`lfDrop`) in `lfLayers()`, `lfMicroLayers()` and
  `srvSetFilter()`, so a half-removed group can never be handed back to a dead renderer.
- **`make validate` is a pre-commit check again.** It samples **2 areas per indicator** for the
  source links. The full sweep — all **165** Outlook links fetched and every displayed value
  recomputed from the response, ~15 minutes — moved to its own target, **`make links`**, to be run
  before a release. `make validate-links` stays as a deprecated alias. Documented in `README.md`
  and `docs/RUNBOOK.md`.

## v2.5 — 2026-09-23

Two layers and a projection: what is **around** a property (services), what a municipality or
quarter is **projected to become** (outlook), and how much housing actually **got built** there.

### Population outlook — DST and Københavns Kommune

- **Ten Outlook indicators** per municipality from **DST's `FRKM126`** 2026 vintage, projected to 2040: total growth to 2040 and to 2031, the change per 1 000 inhabitants per year, the absolute change in persons, and the change in the 0–5, 6–16, 20–34 and 80+ cohorts, plus 20–34 measured **against Denmark's own −7.1 %**. Denmark's own 20–34 population falls over the window, so only **7 of 98 municipalities gain any at all** — the absolute map is a wall of red and the relative one is the map that reads, which is why it carries the chip.
- **The same ten again for Copenhagen's 67 quarters and 10 districts**, from **Københavns Kommune's own `KKFR2026`** — a different run, kept in its own file, its own view and its own legend. **They are never spliced**: DST has the city at +5.9 % by 2040 and KK at +8.3 %, and where both appear the gap is stated rather than averaged away.
- **Net dwelling additions** (`hist_net_dwell`) from `BOL101` + `FOLK1A`: what the dwelling stock actually did, per 1 000 inhabitants per year. It sits in **Housing stock**, not Outlook, because it is a measurement. Its window is **six years, not five** — DST publishes no BOL101 for 2021 or 2022 — and the years are read from the table, never hard-coded. It is shown **beside** the projected population rate, each in its own unit, with **nothing computed between them**: persons are not dwellings without a household-size assumption, and this layer makes none.
- **A diverging colour model.** Zero is a real boundary for these indicators — growth and decline are different phenomena — so the ramp runs `hue_neg → paper → hue_pos` with class breaks **mirrored about the centre**, taken from quantiles of the distance from it. That is also the clamp: `fc_abs` runs from −4 617 to +52 670 and a linear ramp would put almost every municipality in one class.
- **A third direction, `neutral`.** A shrinking municipality is not failing and a growing one is not succeeding, so Outlook indicators are never coloured good/bad, never carry the "↓ lower is better" note, and their ranks are positions rather than verdicts. The Analysis sheet's percentile bar reads the same way for them.
- **The year selector says what it has.** An Outlook indicator is one vintage, so the selector is replaced by *"Projection 2026→2040 · DST 2026"* instead of offering years that do not exist.
- **A projected point never looks like an actual.** Area pages carry a Population outlook card with the observed population solid (`FOLK1A` for municipalities, `KKBEF1` for quarters) and the projection dashed to 2040, split by a *today* marker, with "projected" in every projected point's tooltip.
- **The uncertainty reaches the reader.** KK excludes housing plans from the city total by design, but the **split across quarters is driven by the city's unpublished boligprognose**, which KK itself calls *"behæftet med relativ stor usikkerhed"*. That sentence renders wherever a quarter forecast appears. Copenhagen quarter pages also carry one line of past accuracy from a backtest against eight superseded vintages — Nordhavn's +176 % sits next to KK's own **21.8 %** average five-year error for that quarter.
- **Outlook where the decisions are made**: *Outlook around this project* on every infrastructure datasheet, an **Outlook** section on the Analysis sheet for a pinned address, and a **★ Growth story** saved view — Copenhagen by quarter, 20–34 against the city, infrastructure and public buildings on top — that is nothing but a link, so pasting it restores the view exactly.
- 🚫 **What is deliberately not shown.** The **housing gap** needs a fitted household-size trend, which the hard-data rule forbids — it stays research and is in no build target. Copenhagen's **`fc_netmig`** passes the rule but reverses sign against the stock table for five quarters, two of them the city's biggest development sites, so it is not rendered either. `docs/FORECAST.md` §0a lists both, with the reasons.
- **Verified end to end, every data point.** `scripts/verify_forecast_full.py` re-pulls both source tables with its own request, its own parsing and its own arithmetic and diffs them cell by cell: **22 920 cells and 2 008 recomputed indicator values, 0 mismatches** (`docs/DATA_MAP.md` §7e).

### Services overlay — OpenStreetMap and Rejseplanen

- **Services layer**: groceries, food & drink, pharmacies and public-transport stops — **44 181 points across all 99 municipalities**, 2.6 MB of processed JSON. **National from day one**, unlike the public-buildings and schools layers: nothing here is gated behind a per-municipality API pull.
- **Sources chosen by measurement, not assumption** (`docs/SERVICES_PROBE.md`, `scripts/probe_services.py`). The **Geofabrik OSM extract** (495 MB, ODbL 1.0) gives every shop, eating place and pharmacy in **87 s offline**; **Rejseplanen's static GTFS** (55 MB, CC BY 4.0, no key, no registration) gives the stops. **Overpass was rejected as a runtime dependency**: both healthy mirrors returned HTTP 504/429 for minutes at a time, a boundary-by-name lookup timed out every attempt, `overpass.osm.ch` serves no areas database and answered **0 with HTTP 200 for every tag**, and `kumi.systems`' snapshot was 4.5 months stale. Two of those zeros were wrong and were caught only by the extract. **Fødevarestyrelsen's Smiley feed was rejected too** — 58 718 rows with **no coordinate field of any kind**, counting every food *business* (canteens, wholesalers, kiosks) rather than places a resident walks into.
- **Pipeline**: `scripts/fetch_services.py` (stdlib `urllib`, streams to a `.part` file so an interrupted download leaves no truncated pbf, records url/licence/size/fetch-time in a manifest, skips a copy younger than 7 days) → `scripts/build_services.py`. Raw files are gitignored; the processed JSON is committed, so `make build` and CI need neither the 495 MB download nor `osmium`.
- **OSM**: nodes **and** ways, ways placed at their node centroid — **46 % of Danish supermarkets are mapped as building polygons**, so a node-only read loses nearly half the stock. One point per OSM id: 27 objects nationally carry two of our tags (a petrol-station shop that is also a takeaway counter), and the order `grocery → pharmacy → food` decides which category each lands in. `shop=discount` is **0 nationwide** — Netto, Rema 1000, Lidl and Aldi are all `shop=supermarket`.
- **GTFS modes** come from `stops → stop_times → trips → routes`; `stops.txt` says nothing about them. The **extended** route types are mapped deliberately: without `109` and `700` the entire S-train network and 479 bus stops vanish. `715` (flextur) and `4` (ferry) are excluded and counted in the report. A stop serving several modes becomes one point per mode, so Nørreport is a metro point *and* an S-train point *and* a rail point.
- **Station clustering is ours, because the feed has none**: all 36 383 stops carry `location_type=0` and an empty `parent_station`. Same normalised name + same mode within **150 m** (50 m for buses, enough to merge both sides of a street). **Heavy rail barely clusters** — metro, S-train and rail are already one row per station in this feed — while light rail goes 173 → 109 and buses 34 845 → 24 501 (−30 %). Worked examples for Nørreport, København H and Aarhus H in `docs/SERVICES_COUNTS.md`.
- **Sanity checks fail the build**: København groceries **513** (the probe's figure exactly, ±5 % allowed), metro stations **44** (40–50 expected), no municipality without a grocery, and the run stops before committing anything over 15 MB. All four pass at **2.56 MB**.
- **Map overlay** beside *Infra projects* and *Public buildings*, **off by default**, on the same code paths: the same toolbar toggle, the same legend box in the `.maplegs` column, the same *legend is the filter* interaction (click to toggle, shift-click to isolate, *All* to reset), the same two-level popup. Filter chips: **Groceries · Food & drink · Pharmacy · Transport**, with Transport split into **Rail & metro** and **Bus** because those halves are 628 stations against 24 412 stops. Defaults on switch-on: Groceries + Pharmacy + Rail & metro.
- **Performance first.** Zoom floors per category — rail/metro **10**, groceries and pharmacy **13**, food and bus **14** — each with a muted *"Zoom in to see …"* hint naming what is waiting, including the Bus sub-toggle, which would otherwise show nothing and say nothing. Only points **inside the viewport** (padded 15 %) are drawn, and a redraw happens only once the map leaves that padding, so an ordinary pan costs nothing. Per-kommune files are fetched only for bboxes meeting the viewport, capped at 24 per pass and never below the lowest active zoom floor — the national view meets all 99 boxes, and fetching 2.6 MB to draw nothing is what this layer must not do. **Worst case measured: zoom 14 over Nørrebro with every category on is 3 346 markers rebuilt in 15.6 ms, pan 0.4 ms.** No clustering library: the zoom floors already bound it, and `Leaflet.markercluster` would mean a third-party runtime dependency in a page that vendors Leaflet locally.
- **Fix — the dots were rendering under the choropleth.** A canvas renderer's element is created when the renderer is constructed, and `LF.canvas` is built at map init, before the area polygons exist, so the services canvas sat beneath them and the semi-transparent fill washed every marker out — grey over a dark quintile, correct just off it. Services now draw into their own pane, **`srvpane` at z-index 450**, above `overlayPane` (400) and below `markerPane` (600).
- **Fix — all 99 files loaded on open.** The first load pass ran while the map was still at the national default view, where the viewport meets every bounding box. Loading is now gated by the lowest active zoom floor and capped, nearest first: 12 files in practice over Copenhagen.
- **Touch**: dots and station circles are drawn larger on a coarse pointer, which is also their hit area; the filter chips become pills with a filled on-state and the legend goes full width under 900 px.
- **Attribution** wherever the points are: each popup carries its own source line (`© OpenStreetMap contributors, ODbL` or `Rejseplanen, CC BY 4.0`) with the as-of date, the map footer gains a **Services:** line while the layer is on, and Market › Sources gains a *Services layer* table.
- **Licence resolved**: Rejseplanen Labs' *Retningslinjer for Labs* states **CC BY 4.0**, closing the question the probe left open. Attribution is required and sufficient; redistribution of our derived, clustered subset is allowed; no share-alike, so it does not entangle with the ODbL side.
- **Scope: points only.** No area-level service indicators — OSM density varies enough between Copenhagen and rural Jutland that a per-area rate would rank mapping effort as much as service provision. The per-kommune files are already shaped for it, so the decision is reversible without a refetch.
- Method and caveats: `docs/SERVICES.md`; counts: `docs/SERVICES_COUNTS.md`; source probe: `docs/SERVICES_PROBE.md`; integration notes: `docs/INTEGRATION_services.md`.

### Verify at source — and outlook wording that says what it means

- **Every official figure carries a "Verify at source ↗" link** that opens the publisher's own query for that exact area, so a reader can check the number rather than trust it. For Copenhagen quarter 20104 that is literally `https://api.statbank.dk/v1/s30/data/KKFR2026/CSV?lang=en&OMRKK=20104&Tid=2026,2031,2040`. Indicators with no per-area API fall back to the publisher's page.
- **A link that 200s but disagrees is worse than no link**, so `scripts/check_source_links.py` does not merely fetch: for **all 98 municipalities and 67 quarters** it rebuilds the URL the page builds, fetches it, and **recomputes the displayed value from the returned cells**. Full sweep: **165 Outlook links, 0 bad responses, 0 value mismatches.** The quick form is wired into `make validate`; the full sweep is `make validate-links`.
- **The right table for the level.** An indicator can list several tables — `growth` is FOLK1A (municipal) *and* POSTNR1 (postal) — and sending a postal code to a municipal table is an HTTP 400, not a wrong answer, so the query is chosen by the **area variable's id** rather than by list order. Tables with a variable that cannot be eliminated (INDKP101 `ENHED`, IFOR22 `DECILGR`, BOL101 `BEBO`) carry the registry's own `vars` into the link, so the link reproduces the figure instead of a different slice of it.
- **The rate indicator now says what it is.** `fc_pop_rate_5y` was a per-1 000 rate that read like a percentage; it is now **"Projected population change 2026→2031, % per year"** in `% / yr`, and the headline reads *"Projected change 2026→2031: −492 residents (−1.3 %/yr)"* — the absolute change and the rate together, so neither can be mistaken for the other.
- **The Copenhagen caveat is short and reader-facing.** KK's own warning that the split across quarters is *"behæftet med relativ stor usikkerhed"* reaches the reader in one sentence, with no repo paths in it.

### Map interaction — granularity, filters and the Public layer

- **The area level now follows the zoom.** One ladder — **Country → Municipality → District → Buildings** — replaces the old two-step rule, and the map subdivides itself the way the *Zoom to <kommune>* button does on click: past zoom 10 it adopts the municipality under the middle of the screen, so Copenhagen arrives as **67 quarters** without anyone pressing a button, and a pan across a border re-adopts the new municipality. Past zoom 14 it drops to **individual buildings** where a BBR pull exists. Every rung is a level a publisher actually prints; there is **no interpolated grid**, because a number nobody published is not a figure this dashboard may draw.
- **The ladder never undoes a choice.** A municipality reached explicitly — the *Zoom to* button, a popup link, `/map/<code>` in a shared link — is not the ladder's to release, and neither is a hand-picked *Areas* / *Buildings* setting; only rungs the ladder entered are left automatically. Thresholds are asymmetric (enter at 10 and 14, leave at 9.5 and 13.5) so a half-step of zoom cannot flip the level back and forth.
- **A level label** sits at the map's bottom-left saying what is drawn — *District · Copenhagen quarters* — and what one more zoom step would give.
- **Filters for a pinned test property.** With a pin on the map a **Within: Any / 500 m / 1 km / 2 km / 5 km** control appears, and every point layer is cut to that radius: infra projects (a line or area counts as in range when any part of it is), public buildings, services and the building dots. Over Nørreport at 500 m that is services **400 → 17**, public buildings **1 338 → 125** and buildings **14 575 → 418**. It is plain great-circle arithmetic on published coordinates, it draws its own solid ring, and it lives in the hash as `rad=`, so it survives a reload, a zoom and every rung of the ladder.
- **The Public buildings layer no longer stalls on large municipalities.** Markers moved to a canvas renderer in their own pane (**`pubpane`, z-index 440**), only rows inside the padded viewport are built, and the rest are grid-clustered. DOM nodes **5 293 → 123**, a rebuild **180 ms → 73 ms**, a guarded pan **0.2 ms**. Clustering at 44 px *raised* the cost to 228 ms — 265 permanent tooltips are 265 DOM nodes — so cells are **72 px** and a cluster is labelled only from four members up: 122 clusters and 97 tooltips at zoom 13 over Copenhagen.
- **Toolbar refreshes in place.** The map header is rendered from its own function so the ladder can update the toggles, the explain block and the municipality strip without re-running `lfInit` and tearing a live map down mid-gesture.
- **Two quick-select chips removed** from the indicator bar — *Growth story* and *20–34 KBH* — and the **Population outlook card moved above the area profile** on the property card, where the strongest view in the tool belongs.

## v2.4 — 2026-09-23
- **Test property pin**: paste a Google Maps link — or just `55.67610, 12.56830` — into the box on the map toolbar and the dashboard drops a pin, drills to its municipality at postal-code level and fits the view to a 1 200 m ring. Rings at **500 / 1 000 / 1 200 m** are drawn dashed and non-interactive, so a click still reaches the polygon underneath.
- **The parser reads the link, it does not follow it** (`src/testprop.js`, 15 cases under `make test-js`). `!3d<lat>!4d<lon>` — the place pin — **wins over `@<lat>,<lon>,<zoom>z`**, which is only the viewport centre and can sit a street away; then `q=` / `ll=` / `query=`, `/search/<lat>,<lon>` and plain coordinates with a comma, semicolon or space. `+` and `%2C` are decoded first. A coarse 54–58 N / 7–16 E box catches a swapped lon/lat pair.
- **Short share links cannot be resolved in a browser** and are not pretended otherwise: `maps.app.goo.gl` and `goo.gl` return a named error asking for the long URL from the address bar. Every error is an inline line under the input — never an `alert()`.
- **The exact municipality comes from its own rings**, not from the postal code: `dist/geo/kommuner_lookup.json` (99 kommuner, simplified 0.0005°, 911 kB / 285 kB gzipped) is fetched lazily on the first pin. **Holes are kept** — Frederiksberg is a hole in København, and without it every Frederiksberg pin would be labelled København. If the fetch fails the pin falls back to the postal code's municipality and says **"approx."** rather than guessing silently. A point no polygon covers is reported as *in water or outside Denmark*.
- **Pin popup** in the existing map vocabulary: an editable label (default *Test property*, it travels in the link), kommune · postnr · Copenhagen quarter, the headline row for the pin's finest-level area, and *Analyse ›* / *Copy link* / *Remove*. The pin lives in the hash (`&pin=<lat>,<lon>&pl=<label>`), so it survives a reload, a level change and an indicator change.
- **Analysis sheet** (`#analysis?a=<lat>,<lon>&la=<label>`): one property read against every layer the dashboard already carries. The left nav group is now **Analysis**; the item opens the pin that is on the map, or an empty state with the same Google Maps box — a pin dropped there goes straight to the sheet, no detour via the map. *Analyse ›* in the pin popup lands here and the Back button returns to the map with the pin intact.
- **Sections**: an editable title with kommune · postnr · quarter and the headline row for the pin's finest-level area; a zoom-only mini map with the 500/1 000/1 200 m rings, the infrastructure overlay and the current choropleth underneath; the **area profile** — every indicator that has a value, grouped as in the dropdown, each row with a **direction-aware percentile bar** (it always fills toward "better", so a low burglary rate and a high income read the same way) next to the kommune and Denmark figures and a ↗ into Charts; **Safety** as its own card (plus `safe_pct` for Copenhagen quarters); **Infrastructure nearby**; **Public buildings** and **Schools within 1 000 m**; and **Sources & as of**.
- **Distances are computed, not looked up**: a station Point is a great-circle distance, a line or a polygon ring the nearest point on its segments, and a pin inside a development area is 0 m. Projects within 3 km are listed nearest first; a station that has **not opened** and is inside the 1 200 m ring becomes a headline chip (*M5 phase 1 · v/Prags Boulevard Øst · 200 m · 2036*). Opened projects are listed but never highlighted.
- **Neighbouring municipalities are read too**: the 1 000 m circle is tested against every kommune bounding box, so a pin near a border picks up the buildings and schools on the other side (Lyngby reads Gentofte and Gladsaxe). Outside the 19-kommune Copenhagen metro set both sections say so instead of showing an empty table.
- **Nothing blocks anything**: the header, map, profile and safety render from the inlined data; the per-municipality building files and the school records fill their own sections in place when they land, with a skeleton until then — no re-render, so the mini map is never torn down under the reader.
- Inherited figures keep their `°` and are ranked against **municipalities**, not against the postal codes that all copy the same number; bydel-level quarter figures keep their `^`.
- **Fix — the Public buildings legend is never empty and never loses its categories.** Two faults, one cause: the legend was only ever written at the end of `lfPublicLayers()`, which returns early whenever the layer is off, so every re-render left the container in the DOM with no content — and `.maplegend` paints a 180 × 17 white bar. And it was built from the *active* filter rather than from the category list, so isolating Education switched the box to the FP9 ramp with no category rows at all: nothing on screen could turn Health back on. Now the legend always lists all four categories — an inactive one greyed, struck through and with a hollow swatch, still clickable — *ONLY* still isolates, the grade ramp is appended **under** the categories instead of replacing them, and a box with nothing to say is hidden (`.maplegend:empty`), not left blank. The same guarantee now covers the Infra and Buildings legends.
- **All categories off is a real state.** It used to serialise as `&pub=` and parse straight back to "all", so a copied link reopened showing everything. It is now `&pub=none`, and the legend says **All categories hidden · Show all** with the counts line reading *nothing drawn*. An empty or unreadable `pub=` still means "all", so a hand-typed link cannot blank the map.
- The two overlay legends now **stack in one scrolling column** at the top right instead of the public one sitting at a fixed offset above the infra one — in grade mode it is 441 px tall and the two overlapped.
- **Layer toggles on the Analysis sheet's mini map** (`#analysis?…&lay=infra,public,buildings`): the same three pills as the Macro map, the same styling, the same legends — including the fixed Public buildings legend with its category filters — and the same popups, whose *Open … sheet ›* links are rewired on `popupopen` as they are on the main map. Schools are not a fourth pill here either: they ride inside Public buildings, and isolating Education recolours the school markers by FP9 grade on both maps.
- **Defaults and coverage**: Infra on; Public buildings on where the 19-kommune BBR pull reaches the pin, otherwise the pill is disabled with *Not covered yet: Copenhagen metro area only*; Buildings off, and `micro/<kommune>.json` is fetched only when the pill is switched on — disabled with the municipality named where there is no building file. A file landing refills the mini map in place, so an open popup and the reader's zoom survive it.
- **The mini map follows the headline tile**: clicking GROWTH / UNEMP. / RENTED … recolours the choropleth underneath, as on the map, and the sheet's hash now carries `ind=`, `y=`, `lay=` and the public filter, so **Copy link reproduces the view**.
- **Fix**: the Leaflet canvas renderer for the building dots was cached across map rebuilds on both maps, so a redraw after a re-render threw `Cannot read properties of undefined (reading 'save')`. One renderer per map.
- **Fix**: the OSM name join for public buildings accepted anything tagged `name=`, and a Dataforsyningen WMS endpoint tagged that way in Fredensborg was shipping as a building name in three rows of `data/processed/public/0165.json`. Names that look like URLs are now rejected; one of the three buildings picks up its real name, *Bibliotek og Medborgercenter Hedemarken*.
- **Privacy line under the input**, on both the map toolbar and the Analysis empty state: *"Processed in your browser. The location is stored only in the page URL; don't paste confidential deal locations if you share the link."* It is the accurate statement — the parser never follows the link, the page has no backend, no analytics and touches no browser storage, but the pin rides in the hash precisely so a view can be shared, and anyone given the link gets the coordinate.
- **Verification** (`docs/DATA_MAP.md` §7d): the sheet rendered and read in the browser for three pins on 2026-09-23 — 2450 København SV (reads København **and Tårnby**), Nørrebro, and Esbjerg (both metro-only sections say *"Not covered yet"*, the Public buildings pill is disabled); a cold load straight on an `#analysis` link with no visit to the map first; the pin flow end to end including Back; the short-link and Øresund refusals; and the legend states. The nearest station in the layer, `M5: v/Bryggebroen` at **2 325 m** from 55.64820, 12.53740, is logged with its coordinates for a ruler check, and recomputed by hand to 2 320 m.
- Method, accepted link formats, the short-link limitation, kommune/postnr/quarter resolution, the distance method, the hash parameters and coverage: [`docs/ANALYSIS.md`](docs/ANALYSIS.md); source catalogue entry: `docs/DATA_MAP.md` §11.

## v2.3 — 2026-09-23
- **School quality from Uddannelsesstatistik.dk (STIL)** on the Education buildings of the Public buildings layer: FP9 grades (bundne prøver, dansk, matematik), the socioeconomic reference and its difference, elevtrivsel (generel plus the four sub-indicators), elevtal by herkomst and klassekvotient, for **364 grundskoler** in the 19 Copenhagen metro municipalities over the three latest school years (2023/24 · 2024/25 · 2025/26).
- **API discovery**: the statistics API turned out to expose a full cube catalogue at `POST /Api/v1/skema` — found via the OpenAPI spec at `/swagger/v1/swagger.json`, and documented nowhere in `/GetStarted`. `scripts/fetch_uddstat.py --skema` walks it, so no cube code in this layer was guessed.
- **The socioeconomic reference does not come from the cubes named after it.** `GS/SOCR/SOCREFEX` and `GS/SOCR/SOCREF3ÅR` are listed and return **0 rows at every detaljering tried**; `SOCR/KVALSOC` reports no dimensions at all, and the `SOCREFSIKK` emne is literally named *SocRef_Bag_Login*. The figures come from `OVER/OVERSKO` instead, which publishes the same four per afdeling. It uses a **different verdict vocabulary** — `Over niveau` / `Under niveau` / `På niveau`, not `Bedre/Dårligere end forventet` — and mapping the wrong one silently labelled all 38 schools with a significant difference as "as expected" until a browser check caught it. No 3-year reference is available, and the layer does not fake one from the 1-year figures.
- **Join**: institutionsnummer → STIL institution register coordinates → every BBR building with anvendelse **421** within 150 m (campus rule: all of them, not the nearest). 325 of 364 schools match on 421, another 24 on a 420/429 fallback at the same radius — **95.9 % with geometry, 1 236 school↔building links**. Widening the radius to 250 m was measured and rejected in favour of widening the code set. `bbr_match` records which rule fired.
- **Fixed a silent municipality drop**: the register writes `"Københavns Kommune"` (genitive), so stripping `" Kommune"` never matched `"København"` and København's 135 schools were absent from the first intake pass — its counts said 229 schools where there are 364. A trailing-"s" heuristic would break Assens, Horsens, Randers and six others, so the one exception is spelled out in `KOMMUNE_ALIAS`.
- **Indicators** in a new *Schools* group, kommune + postnr, all hidden from the chip row: `school_grade_avg`, `school_socref_diff` (new `signdec1` format — signed grade points, no % sign), `school_trivsel`, `pupils_per_school`. Area aggregates are **pupil-weighted over folkeskoler and frie grundskoler that publish, specialskoler excluded**, and each measure is weighted by *its own* denominator (grades by the pupils who sat the exams, trivsel by the pupils who answered) — which puts **17 of 19 municipalities within ±0.1 of STIL's own published kommune figure**, against 15 when weighting everything by the total roll.
- **On the map**: an Education building linked to a school gains a school block in its popup — grade with kommune and Denmark beside it, the reference difference with its significance mark, well-being, pupils, class size — and every building of a campus shows the same block and says which of N it is. New **school datasheet** (`#school/<institutionsnummer>`) with a three-year table, benchmarks and the linked buildings; a school panel per area sorted by grade; `schools 8,4 avg` on the area card's PUBLIC line; and a *Schools* trend card in Charts per selected municipality against Denmark.
- **Grade colour mode**: with the public filter on Education alone (`&public=1&pub=edu`) the Education markers take a 5-step blue ramp by FP9 grade, quintiles over the loaded municipalities, and the legend switches to *FP9 grade avg*. **A school with no grade keeps the base Education hue, hollow with a thin outline, never the bottom bin** — 0.–6. klasse schools, specialskoler and suppressed cells are not low-scoring schools.
- **Suppression is not zero**: a cell under 3 observations (under 5 pupils for trivsel and the reference) is *omitted* from the API response, so a missing school can only be counted from the outside. Coverage is 74 % of schools for grades, 70 % for the reference, 54 % for trivsel; every dash in the UI carries a tooltip saying it is suppressed, never a 0.
- Licence: free reuse including commercial, attribution **"Kilde: Uddannelsesstatistik.dk"** plus retrieval date — carried on every popup, sheet, panel and chart, and in the sources table.
- **Verification** (`docs/DATA_MAP.md` §7): five schools — Skolen på Duevej, Lindevangskolen, Den Classenske Legatskole, Krebs' Skole and Nørre Fælled Skole — re-queried one at a time straight from the API on 2026-09-23. All 20 comparisons identical to `data/processed/schools.json`, including a suppressed well-being cell that is absent in both and both significance directions.
- Method, cube codes, the measured join and the discretion rules: `docs/SCHOOLS.md`; source catalogue entry: `docs/DATA_MAP.md` §10.

## v2.2 — 2026-09-23
- **Public buildings overlay** from BBR via Datafordeler: 7 517 school, daycare, health and culture buildings (`byg021BygningensAnvendelse` 410–449) in the 19 Copenhagen metro municipalities — 6 769 standing buildings with floor area and year built, plus 257 buildings with an **open building case** whose permit is three years old or less. Coordinates from BBR, and from DAR for the 83 % of open-case buildings BBR has not placed; names from OpenStreetMap where one sits within 60 m (30 % of the set).
- **Why "open building case" and not "planned"**: of 290 open cases in the pilot only 1 carried an expected completion date, 91 % had a permit date and the median permit in København was 4.1 years old. The layer reports case activity with that caveat on every popup and sheet, and ships no forward-looking pipeline indicator. Older open cases are counted in the area panel under *Stale open cases* but never drawn.
- **On the map**: a *Public buildings* toggle beside *Infra projects* (`&public=1`), markers hued by category — education, daycare/institutions, health, culture — filled for existing and hollow-dashed for an open case, with a white halo, wide hit area and names as labels from zoom 14. Density by zoom: open cases only below 9, open cases plus buildings ≥ 1 000 m² from 9, everything from 13; per-municipality files load for whatever is in the viewport.
- **Legend filters**: the category rows and the existing/open-case row are toggles (shift-click or *only* to isolate, *All* to reset) and the filter travels in the URL (`&pub=edu,health&pubkind=existing`), so a filtered view can be shared. The legend count follows the filter.
- **Building sheet** (`#public/<kommune>/<id>`) reusing the project-sheet layout, popups with address, area, year or permit/case age, and an area **PUBLIC** line on the card whose segments open a filtered list panel.
- **Indicators** in *Growth signals*: `public_m2_per_1000` (existing floor area per 1 000 inhabitants) and `public_recent_cases_n`, both hidden from the chip row and both carrying the coverage and owner-reported caveats.
- Verification (`docs/DATA_MAP.md` §7): five buildings re-queried one by one from BBR on 2026-09-23 — code, floor area and year built identical in all five.

## v2.1 — 2026-09-23
- **Infrastructure overlay**: 51 curated projects that will change accessibility — state transport projects with budget, original and current opening year from *Transportministeriet, Anlægsstatus 1. halvår 2026*, alignments from *Fingerplan 2019* (Greater Copenhagen) and OpenStreetMap, plus hospitals (kvalitetsfondsbyggerier), BRT lines, university campuses and state buildings from the regions, Movia, Vejdirektoratet, municipalities and Bygningsstyrelsen. Every row carries its own source link; a figure the source does not state is left empty, and a geometry drawn by hand is flagged `schematic`.
- **On the map**: an *Infra projects* toggle that survives every level change and rides in the URL (`&infra=1`); four status tones (study dotted · decided dashed · under construction solid · opened thin grey), hatched development areas, station markers with a white halo, wide invisible hit lines so a dotted study corridor is easy to click, clickable labels (short name zoomed out, full name from zoom 11), and a status legend above the indicator legend.
- **Project datasheet** (`#project/<id>`): opening year or window, budget in bn DKK with its price-basis caveat, length / stations / area computed from the geometry, a mini map over the current choropleth, the municipalities, postal codes and quarters served as links, source, notes and the schematic warning.
- **Upcoming on area cards**: the three nearest non-opened projects with their year, and a link to the rest.
- **Pipeline** (new nav item): every project including the ones kept off the map, sortable, filtered by type and status, with its own CSV; a row opens the project on the map or its datasheet. The *Export data* CSV gained a project block.
- **Growth signals** indicators: `projects_upcoming` and `stations_planned_1200m` per municipality and postal code, from a spatial index that also counts stations and sites within 1.2 km.
- New field `open_window` for projects whose source gives a span instead of a year (Nyt Hospital Bispebjerg 2030–2032, Universitetsbyen Syd 2028–2031).
- Verification (`docs/DATA_MAP.md` §7): five projects re-read against their sources on 2026-09-23. Femern's road opening year was removed — the cited Femern A/S article states no year — and Nyt Hospital Nordsjælland's budget citation now names the report that carries the figure.

## v2.0 — 2026-09-22
- **Safety indicators** from Statistics Denmark — `STRAF11` (reported offences by place of offence, quarterly from 2007) and `STRAF22` (reports and charges, annual): reported crime, violent crime, property crime and drug/weapons offences per 1,000 inhabitants, residential burglaries per 1,000 dwellings, reported crime y/y and the share of penal-code reports with a charge. Rolling four quarters, municipality level; postal codes and Copenhagen quarters inherit the municipality value (°). Registry fields `direction`, `note`, `chip`, `history_from`, `map_from`, `breaks`.
- **Map and table**: *Crime* chip, *Safety* group in the indicator select (terracotta ramp), fifth figure *Crime* in the municipality card, Safety block in popups and on area pages, Safety columns in the table while a Safety indicator is selected; the details line shows the registry note and "Danmarks Statistik, STRAF11 / FOLK1A, as of 2025K3→2026K2, fetched …".
- **Direction-aware ranks**: "#n of 98" counts from the best end — for *lower is better* indicators (Safety, unemployment) #1 is the lowest value; y/y and vs-median colours and the first click on a table column follow the same sense; legends say "↓ lower is better · darkest = highest".
- **Charts**: axis starts at each indicator's first year (crime 2007); *Yearly | Quarterly* toggle for rolling-quarter series (2008 Q1 → latest); overlays of related indicators; dashed *Denmark* reference line (DST area 000); series breaks from the registry drawn as markers with the source's wording as tooltip; the axis stays at 0 for all-positive series.
- **Year selector**: Safety indicators offer 2008 → latest; rolling indicators whose window ends in the latest year are labelled "2026 (latest)" instead of "latest (2025 data)".
- **Sources**: *Fetched* column filled for every table (date of the raw pull); STRAF11/STRAF22 listed with their `updated` stamps.
- **Copenhagen quarters get their own safety figures** from Københavns Kommunes Tryghedsundersøgelse 2026 (Epinion) / Københavns Politi: reported crime per 1,000 inhabitants (2025) and the new indicator *Feeling safe in own neighbourhood* (2026 survey), published per bydel and shown on all 67 quarters with a `^` marker and the level tag *bydel (KK survey)*. `scripts/import_kk_tryghed.py` extracts the 13 areas from the PDF with cross-checks; `data/external/cph_bydel_map.csv` maps them to the quarters (Nørrebro split into Indre and Ydre, confirmed against the report's populations). Quarter pages and popups also show the survey's reported-offence count and its violence and burglary rates per 1,000 inhabitants.
- **Units**: rates per 1,000 no longer borrow the ‰ sign — "263" on the map and in the legend, "90 / 1,000" in cards, tables, popups and chart legends. Map labels no longer overlap, and a figure published per bydel is labelled once per bydel.
- The infrastructure-projects overlay moves to v2.1.
- Tests: `tests/test_safety.py` (`python3 -m unittest discover -s tests`) — rolling window, suppression, multi-code select, yearly filing, København 89.9 per 1,000 and clearance 17.1 % from the raw pulls.

## v1.9 — 2026-09-20
- **Overview first**: the top bar is a breadcrumb (Denmark › municipality › area — every step a link) and the sidebar shows names only; the duplicate card titles, back-links and "× Denmark" chips are gone, so the map starts ~110 px higher.
- **Indicator picker**: grouped select plus quick chips (Growth · Price/m² · Rent · Unemp. · Rented · Supply) on the map and in the table; the indicator's definition, source, coverage and history are folded behind *ⓘ details* (one line by default).
- **Map colours and legend**: five quintile classes instead of a min–max ramp (each class holds a fifth of the visible areas, so outliers no longer flatten the map), a stronger ramp, and a class-break legend drawn on the map (bottom right) for the macro map, the buildings layer and the area-page map. Building shares (rented, unoccupied, < 50 m²) use fixed breaks. Fewer, cleaner labels: the 12 largest municipalities at national zoom, values from zoom 8, no overlapping names.
- **Area pages**: headline row with the five key figures (value, y/y, rank) under the title; tiles show one theme at a time (Demographics first) with a single context line (#rank · vs median) and a ↗ into Charts; the explanatory paragraph became an ⓘ tooltip; *All indicators*, *Housing stock (BBR)* and *Quarters & postal codes* are tabs of one card — the page is about 2 200 px instead of 5 000.
- **Map popups** in two levels: the selected indicator big with its rank, four headline figures, buttons *Open page › · Zoom to municipality · Buildings › · ↗ Chart*, and *All N values* folded underneath. Building cards name their quarter / postal code with a link to it.
- **Buildings**: address search first, filters folded behind *Filters (n active) ▾*.
- **Charts**: the empty state explains what to do and offers the quick picks; the "single snapshot" note only appears when it applies; every tile, table row and popup has a ↗ that opens Charts with that area and indicator.

## v1.8 — 2026-09-18
- **Addresses and property numbers on buildings**: `scripts/fetch_dar.py` pulls the access address from DAR (husnummer → street + postal code) and the BFE property number from BBR (building → grund → ejendomsrelation) for every building in the Micro layer — 174 539 / 174 551 buildings (100 %) got an address, all got a BFE. Building cards show the address as title, the BFE and how many other buildings share the property; CSV export carries both columns.
- **Find address** box in Buildings mode: type "Nordens Plads 4" → the map zooms to the building and opens its card (filters are widened if the building was filtered out).

## v1.6 — 2026-09-15
- **Charts** (new nav item): pick an indicator, up to 8 areas (municipalities, postal codes, Copenhagen quarters — search box or quick picks), year range and a median line; download as PNG (2400×1280, self-contained with title, legend and source) or the data as CSV; the URL holds the whole setup.
- **Sources** merged into Market: folded panel *Sources, freshness and indicator definitions* at the bottom (`#market?src=1` opens it).
- **Full-screen map**: ⤢ button on the map card (Esc exits); works in Areas and Buildings mode.
- Charts: lagging sources no longer leave a gap before an isolated "latest" point — series end at the last year with data and the legend shows that year; **Bars** mode (latest value per area, median marker) for indicators without a time series; **Distribution** mode: BBR donuts per area (dwelling size, rooms, year built, building type) with CSV; Auto picks line/bars.
- Map popups are wider with two columns — all indicators visible without scrolling; building popups likewise.
- New count indicators next to the normalised ones: *Dwellings completed, last 4Q (number)* and *Homes for sale (number)*.

## v1.5 — 2026-09-15
- **Buildings (Micro) layer** inside the map view: when a municipality is open, *Areas | Buildings* switches to every residential building with ≥ 2 dwellings as a dot (size = dwellings, colour = rented share / unoccupied / average size / year built / dwellings / < 50 m² / floors), filters (min. dwellings, year built, building type, rented ≥ %), building popup with tenure, sizes and rooms, CSV export of the filtered set, link from area pages. `scripts/build_micro.py` writes one compact file per municipality (`data/processed/micro/<kommune>.json`, loaded on demand — the page must be served over http: `make serve` or GitHub Pages).
- BBR coverage: Copenhagen and the 18 suburban municipalities (776 791 dwellings); the rest of the country follows as it is fetched.

## v1.4 — 2026-09-14
- **BBR housing stock at postal-code and quarter level** (Datafordeler GraphQL, free API key): `scripts/fetch_bbr.py` pulls current dwellings and buildings per municipality (resumable, `--metro` / `--all`, `--workers`), `scripts/build_bbr.py` places each dwelling by its building's coordinate into postal codes and Copenhagen quarters and writes `data/processed/bbr.json`.
- Seven new indicators, group *Housing stock (BBR)*: rented share, unoccupied share, average size, share < 50 m², built 2010+, multi-dwelling share, dwellings per building — same definition at every level, so no inherited ° values. Validation: Frederiksberg 57 942 dwellings vs DST BOL101 57 576 (+0.6 %).
- Area pages: *Housing stock — BBR register* card with rooms, size, year-built and building-type distributions (municipality as reference tick).
- Coverage grows as municipalities are fetched (first: Copenhagen and 18 suburban municipalities); areas without a pull show –.

## v1.3 — 2026-09-14
- Map view is map-only: area selector (Denmark / region › municipality), indicator + year, selected-municipality strip with rank and a link to its page. Tables moved to the Table view (own nav item; rows open area pages; Δ column), trend chart removed from the map.
- Area pages for every municipality, postal code and Copenhagen quarter (`#area/<type>/<code>`): key-figure tiles by group (Δ since first year, rank among peers, sparkline), trend chart vs municipality and median of peers, context map (neighbours clickable), all-indicator comparison table, sub-area table (postal codes / quarters).
- Navigation by URL hash — every screen has a permalink, browser back/forward and Esc work; popups link to area pages; legend shows min/max; polygons highlight on hover; faster wheel zoom.
- Map view: searchable area box (type a municipality, postal code or quarter; Enter opens it), source list folded under *Data information*.
- Area page tiles: *All figures* first (max 14 = 7 × 2), each tile with y/y change, sparkline against the median of peers (dashed), rank bar, change since 2016 and gap to the median.
- Responsive layout for laptops with 125–150 % display scaling (Windows): narrower sidebar and type below 1400 px, map height follows the window, tile grid 7 → 5 → 4 columns, stable scrollbar gutter; a drilled-in municipality shows its sub-areas at any zoom.
- Sidebar: renamed *Macro Dashboard*; footer replaced by **Export data** — one long-format CSV (level; code; name; parent; region; population; year; indicator; label; unit; value; as_of) of every level, indicator and year plus the macro series.

## v1.2 — 2026-09-14
- Copenhagen quarter level: 67 kvarterer from Københavns Kommune's statbank (`s30`, config section `cph`, `scripts/build_cph.py`) with 12 indicators and 2016–2026 history; polygons from the municipality's WFS (`scripts/fetch_geo_cph.py`, CC BY 4.0).
- UI: clicking København shows quarters with a *Quarters | Postal codes* toggle; table mode gains a *Copenhagen quarters* level; popups, trend chart, CSV export and year selector work on quarters.
- Map: fractional zoom so a municipality fills the map after drill-down; labels rebuilt on every zoom step and shown only where the polygon is wide enough on screen (fixes the label pile-up in Copenhagen).
- Unemployment in Copenhagen is district-level (bydel) by source — repeated on each quarter and flagged in the indicator explanation.

## v1.1 — 2026-09-14
- Time series 2016–2026 for every indicator (same calculation per year, same quarter/month each year; values filed under the data's own year so lagging sources do not repeat).
- Year selector next to the indicator selector; map, tables, popups and CSV follow the selected year; "Δ since 2016" column.
- Trend card: selected indicator over the years for pinned municipalities (☆ in the table) or the five largest.

## v1.0 — 2026-09-14
- First public release on GitHub Pages: 99 municipalities, 606 postal-code areas, 20 indicators, 13 macro series.
- Rents: private (boligstat.dk 2026, 95 municipalities) and social (Landsbyggefonden 2026, 98) with reproducible import scripts.
- UI: grouped indicator selector with explanation panel; Map/Table mode with search, region filter, min-population and CSV export; popups survive auto-pan; OpenStreetMap basemap.
- Geometry: postal codes clipped to municipalities, central-Copenhagen street codes merged, dominant municipality by overlap area.
- Verification: 11 cells recomputed independently (docs/DATA_MAP.md §7b).
- CI: every push deploys; monthly data refresh.

## v0.2 — 2026-09-14
- Dashboard template (`src/`): design ported from the Finnish edition, English UI, registry-driven indicators, views Macro map / Market / Sources.
- Pipeline scripts: `validate_config`, `fetch_statbank` (DST + Finans Danmark `s20`), `fetch_geo_dawa`, `build_makro`, `build_market`, `build_dashboard`.
- Docs: `DATA_MAP.md` (sources), `BUILD_PLAN.md` (phases), `RUNBOOK.md` (step-by-step), `DATA_FOLDERS.md` (folder conventions), `GEO.md` (boundaries).
- `Makefile`, monthly GitHub Action with Pages deploy, MIT licence.

## v0.1 — 2026-09-14
- Data map research: Statistics Denmark, Finans Danmark, DAGI/DAWA, BBR, rents, macro sources; key endpoints verified live.
