# Geometry, raster and spreadsheet work — scripts/fetch_flood.py, scripts/build_climate.py
# and the kunta-ring simplification in scripts/build_dashboard.py.
#
# Nothing else in this repository needs these, and the dashboard build does not:
# data/processed/climate.json is committed, so `make build` and the GitHub Actions
# workflow keep working on a plain Python with no extra packages.
#
#   python3 -m pip install -r requirements-geo.txt
#
# Versions are the ones this was actually built and verified with on 2026-09-24, not a guess.
shapely==2.1.2       # mapped-extent unions, kunta ∩ extent, ring simplification
numpy==2.5.3         # the flood masks: 280 M pixels is not a pure-Python loop
pillow==12.3.0       # decodes the publisher's PNG tiles and rasterises our own polygons
openpyxl==3.1.5      # STUK's radon spreadsheets, by kunta and by postal area
